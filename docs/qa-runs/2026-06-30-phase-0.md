# QA Checkpoint #0 — Foundation (Фаза 0)

**Дата:** 2026-06-30
**Ветка:** feat/phase-0-foundation
**Окружение:** Postgres 16 + Redis 7 через `docker compose` (sm-postgres:5433, sm-redis:6380)

## Результат: PASS ✅

## Definition of Done — проверка

| # | Критерий | Статус | Доказательство |
|---|----------|--------|----------------|
| 1 | Монорепо (pnpm workspace) | ✅ | apps/web, apps/worker, packages/shared, packages/db, packages/queue |
| 2 | packages/shared + тесты ≥90% | ✅ | 34 теста, покрытие 97.8% строк / 100% функций / 91.7% веток |
| 3 | Prisma schema + миграции + ERD | ✅ | migration `20260629235805_init`, авто-ERD.md (prisma-erd-generator) |
| 4 | NestJS backend (worker) | ✅ | health-эндпоинт + graceful shutdown, 2 теста |
| 5 | Next.js frontend | ✅ | App Router + /health, 2 теста (100% cov), `next build` OK |
| 6 | Docker Compose (Postgres + Redis) | ✅ | docker-compose.yml, оба сервиса healthy, окружение поднято из файла |
| 7 | CI pipeline (quality-gate) | ✅ | .github/workflows/ci.yml: typecheck+build+test:cov+anti-weakening |
| 8 | Husky pre-push | ✅ | .husky/pre-push (typecheck+test), core.hooksPath=.husky/_ |
| 9 | QA checkpoint #0 | ✅ | данный отчёт |

## Прогон тестов (против compose-окружения)

```
packages/shared  6 files  34 passed
packages/db      1 file    2 passed   (multi-tenant isolation, integration)
apps/worker      1 file    2 passed   (health + graceful shutdown)
packages/queue   1 file    1 passed   (BullMQ <-> Redis, integration)
apps/web         2 files   2 passed   (HomePage smoke + /health route)
------------------------------------------------
ИТОГО           11 files  41 passed   0 failed   (exit 0)
```

## Typecheck

`pnpm -r typecheck` → exit 0 по всем 5 проектам.

## Зафиксированные и устранённые проблемы

1. **Дублирование ioredis (5.10 + 5.11)** ломало типы в packages/queue.
   Устранено добавлением `overrides: { ioredis: 5.11.1 }` в pnpm-workspace.yaml
   (в pnpm 11 overrides переехали из package.json в workspace-файл).
   Анти-weakening соблюдён: исправлена причина, проверка типов НЕ ослаблена.

## Анти-weakening аудит

- Запретов `.skip` / `.only` / `xit` в тестах нет.
- CI содержит шаг "No test weakening", блокирующий их появление в PR.
- Ни один тест не отключён и не ослаблен ради прохождения.

## Вывод

Фаза 0 завершена. Фундамент готов к Фазе −1 (de-risk spike Baileys).
